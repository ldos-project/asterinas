#!/bin/sh

# SPDX-License-Identifier: MPL-2.0

# The initramfs entry point (i.e., the init process).
#
# This sets up the basic environment and
# transfers control by executing the command-line arguments of the script,
# which typically either provide a shell for user interaction or
# run tests automatically.
#
# TODO: This script simulates the process of mounting filesystems
# as performed by a generic init process.
# It should later be replaced by the actual init process.

if [ "$#" -eq 0 ]; then
    echo "No arguments were provided to the init process. Spawn a shell by default."
    echo "Usage: $0 <prog> [args...]"
    set -- /bin/sh
fi

mount -t sysfs none /sys
mount -t proc none /proc
mount -t cgroup2 none /sys/fs/cgroup
mount -t configfs none /sys/kernel/config
mount -t oqueue none /oqueues
mount -t ext2 /dev/vda /ext2
mount -t exfat /dev/vdb /exfat

# The userspace RAID-1 selection policy reads and writes OQueues that only exist when the kernel
# was booted with `raid.selection=userspace`; only start the server in that case, and start it
# before mounting `/raid1`, since mounting drives reads that block on the server's first decision.
if grep -q 'raid.selection=userspace' /proc/cmdline; then
    raid_policy_server &
fi

# The OQFS round-trip microbenchmark: when booted with `oqbench.enable`, launch its userspace peer
# (as root) and any `oqbench.busy_procs` competing busy-loop processes, both configured from
# `/proc/cmdline`.
if grep -q 'oqbench.enable' /proc/cmdline; then
    oqbench_peer_compute=$(sed -n 's/.*oqbench\.peer_compute=\([0-9]*\).*/\1/p' /proc/cmdline)
    oqbench_busy_procs=$(sed -n 's/.*oqbench\.busy_procs=\([0-9]*\).*/\1/p' /proc/cmdline)
    i=0
    while [ "$i" -lt "${oqbench_busy_procs:-0}" ]; do
        sh -c 'while true; do :; done' &
        i=$((i + 1))
    done
    oqbench_server --compute "${oqbench_peer_compute:-0}"
    echo "oqbench: peer exited with status $?; powering off"
    poweroff -f
fi

mount -t ext2 /dev/raid /raid1

# When the init process starts,
# it does not have a process group, session, or controlling terminal.
# The `script` tool is a standard Linux utility
# that captures everything displayed on your terminal screen and records it to a file.
# It tricks a program into thinking it is running in an interactive terminal.
#
# TODO: Use `cttyhack` once we support it, which is a better choice.
script /dev/null -q -c "$*"

poweroff -f
